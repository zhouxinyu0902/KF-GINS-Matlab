# KF-GINS-Matlab 目录与代码导航

> 仓库位置：`D:\Github\KF-GINS-Matlab`  
> 整理日期：2026-08-10  
> 说明：本文档依据当前仓库静态阅读整理。仓库包含原始 KF-GINS-Matlab、后续测距/深度组合导航扩展，以及多批仿真和实测研究代码。部分脚本带硬编码绝对路径，运行前需按本文“运行建议”检查配置。

## 1. 项目概览

本仓库以 MATLAB 实现的松组合 GNSS/INS 扩展卡尔曼滤波为基础，逐步扩展了水下/弱 GNSS 环境中的多源组合导航研究，包括：

- GNSS 位置、速度与 INS 组合导航；
- ODO/NHC 辅助导航；
- 单信标、多信标、移动信标测距/LBL 与 INS 融合；
- 深度/高度、方位角和位置等复合观测更新；
- 10 状态、15 状态等不同误差状态模型；
- 前向/反向滤波、RTS、分块和双向平滑；
- IMU 增量优化与 MEMS/i300/ADIS16545 等器件实验；
- 因子图优化（FGO）、固定滞后与近实时滑窗算法；
- 信标可观测度、Fisher 信息矩阵与轨迹构型分析；
- 潜标位置两阶段标定及迭代补偿。

当前目录约含 328 个 `.m` 文件；大量 `.txt`、`.nav`、`.mat`、`.xlsx`、`.png` 和 `.fig` 属于输入数据、计算结果或论文图表，不是独立程序入口。

## 2. 推荐阅读路线

1. `kf_gins.m`：理解标准 EKF GNSS/INS 主循环。
2. `InsMech.m`：理解捷联惯导速度、位置和姿态更新。
3. `GINS-KF/`：理解初始化、误差传播、GNSS/ODO 量测更新和闭环反馈。
4. `function/`：掌握坐标、四元数、地球模型等基础工具。
5. `dataset1/`：查看标准 GNSS/INS 和测距/INS 扩展的完整用例。
6. `function_zxy/`：阅读测距、深度、方位角、多信标、不同状态维数与结果评估的通用扩展。
7. 按研究主题进入 `MEMS_RANGE/`、`new_惯导试验/`、`潜标位置标定/` 等目录。

主流程可概括为：

```text
ProcessConfig* 配置
        ↓
IMU / GNSS / range / depth / ODO 数据读取与时间对齐
        ↓
Initialize / myInitialize* 初始化
        ↓
InsMech + InsPropagate / myInsPropagate* 连续预测
        ↓
GNSSUpdate / my*RangeUpdate / 高度、方位等观测更新
        ↓
ErrorFeedback / myErrorFeedback* 闭环反馈
        ↓
NavResult、状态、协方差输出
        ↓
plot* / calc* 误差统计、绘图和 Excel 汇总
```

## 3. 根目录

| 路径 | 类型 | 作用 |
| --- | --- | --- |
| `kf_gins.m` | 主脚本 | 原始 EKF GNSS/INS 入口；读取 `ProcessConfig*`，执行惯导递推、GNSS 更新、误差反馈和结果输出。 |
| `kf_gins_backup.m` | 备份脚本 | `kf_gins.m` 的历史/备份版本，通常不作为首选入口。 |
| `InsMech.m` | 核心函数 | 捷联惯导机械编排：利用相邻 IMU 增量更新速度、位置、姿态及地球参数。 |
| `pathinit.m` | 环境脚本 | 将仓库及子目录加入 MATLAB path；内部绑定了本机绝对路径，并调用 `savepath`。 |
| `.gitignore` | Git 配置 | 忽略规则。 |

注意：根目录 `kf_gins.m` 当前默认调用 `ProcessConfig1()`，该函数实际位于 `dataset1/`；运行前需要先加入递归路径或切换工作目录。

## 4. 基础算法与公共函数

### 4.1 `function/`：数学与坐标基础库

该目录主要来自原始 KF-GINS-Matlab，依赖少、复用范围广。

| 文件/文件组 | 作用 |
| --- | --- |
| `Param.m` | WGS-84、角度转换、IMU 单位等公共常量。 |
| `getGravity.m`、`getRmRn.m` | 重力和子午圈/卯酉圈曲率半径。 |
| `euler2dcm.m`、`dcm2euler.m` | 欧拉角与方向余弦矩阵互转。 |
| `euler2quat.m`、`dcm2quat.m`、`quat2dcm.m` | 欧拉角、DCM、四元数互转。 |
| `quatProd.m`、`quatInv.m`、`quatNormalized.m` | 四元数乘法、求逆和归一化。 |
| `rotvec2quat.m`、`quat2rotvec.m` | 旋转矢量与四元数互转。 |
| `bl2qne.m`、`qne2bl.m` | 大地坐标与导航系姿态四元数相关转换。 |
| `interpolate.m` | 导航状态/量测的时间插值。 |
| `skew.m` | 三维向量反对称矩阵。 |
| `TestFunction.m` | 基础函数测试。 |

### 4.2 `GINS-KF/`：标准 EKF 组件

| 文件 | 作用 |
| --- | --- |
| `Initialize.m` | 根据配置生成初始导航状态、误差状态和协方差。 |
| `InsPropagate.m` | 误差状态转移和协方差传播。 |
| `GNSSUpdate.m` | GNSS 位置/速度量测更新，含杆臂处理。 |
| `GetOdoVel.m` | 从 ODO 原始数据提取更新时刻附近的速度。 |
| `ODONHCUpdate.m` | ODO/NHC 更新框架。 |
| `ErrorFeedback.m` | 将估计误差反馈到导航状态并重置误差状态。 |
| `Copy_of_InsPropagate.m` | 历史副本，不建议作为默认实现。 |

### 4.3 `function_zxy/`：测距组合导航扩展库

这是仓库中最重要的二次开发公共库。

| 子目录/文件组 | 作用 |
| --- | --- |
| `myInitialize*.m` | 标准、10 状态、15 状态滤波初始化。 |
| `myInsPropagate*.m` | 多种状态模型和 NED 形式的误差传播。 |
| `InsMechBackward.m` | 反向惯导机械编排。 |
| `Update/` | 单/双/三信标测距、LBL、GNSS、方位角+距离+位置以及自适应更新。 |
| `ErrorFeedback/` | 与各状态模型对应的误差反馈，包括无姿态、位置-only 和测距模型。 |
| `beacon/` | 生成单信标、三信标和移动信标数据。 |
| `compute/` | 信标到距离、点到点/批次距离及平面位移计算。 |
| `referconvert/` | ENU/NED、FRD/RFU/FUR 等导航与 IMU 坐标系转换。 |
| `plot/` | 轨迹、信标、状态量和径向误差分析。 |
| `plot-function/` | 导航结果、STD、IMU 误差、轨迹对比和 GJB 4727A 风格误差统计。 |
| `rangedataget*.m` | 由参考轨迹构造不同信标布设下的仿真测距数据。 |
| `myRangeUpdate_imuOpti.m` | 含自适应因子的测距/深度更新。 |
| `Bidirectional_smoother.m` | 前后向结果融合。 |
| `solve_coupled_imus.m` | IMU 角增量/速度增量耦合优化。 |
| `rotateAndScaleTrajectory.m`、`transformTrajectory.m` | 轨迹旋转、缩放和几何变换。 |

`function_zxy/Update/` 中常用接口包括：

- `myRangeUpdate.m`、`myRangeUpdate_m.m`：单路距离/深度更新；
- `my2RangeUpdate.m`、`my3RangeUpdate.m`：双/三距离联合更新；
- `myLBLUpdate.m`：LBL 观测更新；
- `myRange_aziUpdate.m`：距离与方位联合更新；
- `myRange_azi_pos_Update*.m`：距离、方位与位置组合及自适应版本；
- `myGNSSUpdate.m`、`myGNSSUpdate_15state.m`：GNSS 更新扩展。

## 5. 基准数据与基础实验

### `dataset1/`

GNSS 位置/INS 基准数据，也是仓库大量测距扩展的主要起点。

- 配置：`ProcessConfig1.m`、`ProcessConfig1_zxy.m`；
- 入口：`kf_gins_ins_gnss.m`、`kf_gins_ins_gnss_15state.m`、`kf_gins_ins_range.m`、`kf_gins_ins_range_15state.m`；
- 预处理：`dataget1.m`；
- `input/`：Leador-A15 IMU、GNSS/RTK、深度/高度、LBL、静态/移动/多信标距离和真值；
- `output/`：导航结果、纯惯导结果、误差状态和标准差等。

### `dataset2/`

GNSS 位置速度/INS 数据集。`ProcessConfig2.m` 定义 ADIS16465、GNSS-POSVEL 和真值路径，`plot_p_v_2.m` 用于位置/速度结果查看。

### `dataset3/`

ODO/NHC/INS 数据集。`ProcessConfig3.m` 配置 ADIS16465、GNSS 位置、ODO 和真值，`plot_p_v_3.m` 用于结果查看。

### `dataset4/`

i300/ADIS 与多路测距、深度数据实验。

- `ProcessConfig4_zxy.m`：配置文件；
- `ins_range_i300.m`：i300 测距/INS 主脚本；
- `height_get.m`：高度/深度辅助处理；
- `input/`：ADIS16460、ADIS16465、i300、GNSS RTK、depth、range1~3；
- `output/`：MEMS-EKF 与真值结果。

### `dataset-simu/`

直线、方形、Z 字等仿真轨迹数据及 GNSS/LBL/测距组合实验。

- `ProcessConfigsimu.m`：仿真配置；
- `dataget_simu.m`：仿真数据准备；
- `kf_gins_ins_gnss_simu.m`：仿真 GNSS/INS；
- `kf_gins_ins_LBL_line.m`、`kf_gins_ins_range_line.m`：直线轨迹 LBL/测距实验；
- `line/`、`square/`、`zz/`：不同轨迹输入及部分输出。

### `dataset-20230504/`

PHINS/REMSES/USBL/LBL/深度实测数据处理。

- `kf_gins_ins.m`、`kf_gins_ins_gnss_PHINS.m`：导航入口；
- `ProcessConfigPHINS.m`：配置；
- `2023/`：PHINS、REMSES、USBL 日志读取、CSV 转换和 INS 处理脚本；
- `DELPH data/`：DELPH 姿态/速度数据和绘图；
- `input/`、`output/`：导航输入和纯惯导输出。

## 6. 研究专题目录

### `MEMS_RANGE/`

面向 MEMS IMU 与低频测距约束的组合导航实验。

- `ins_range_MEMS_1.m`：综合入口；
- `Azimuth_position/`：由信标/方位约束求位置，以及方位+测距+INS 融合；
- `data_16545/`：ADIS16545 实验；
- `data_i300/`：i300 实验；
- `data_830_430_1`~`data_830_430_6/`：多批实测数据及结果；
- `data_simu/`：仿真与 IMU 信息优化；
- `function/`：MEMS 配置、反向编排、1 秒反向滤波、RTS 后向计算、IMU 增量优化及噪声分析。

### `惯导实验数据/`

实测惯导数据上的 EKF/FGO、测距/深度融合和平滑实验。

- 主脚本：`FGO_gnss_ins.m`、`FGO_range_ins_m.m`、`FGO_range_ins_rad.m`；
- 配置：`ProcessConfig_exper.m`、`ProcessConfig_exper_m.m`；
- `func/`：高度更新、反向解算、RTS/分块/统一平滑及轨迹场景绘图；
- `input/`、`input_pre/`：原始及预处理输入；
- `output/`、`fig/`：导航结果与图件；
- `New Folder/`：临时/试验性文件，使用前应核对来源。

### `基于优化惯性数据/`

先优化 IMU 信息，再进行测距/INS 融合的实验。

- `ProcessConfig_imuInforOpt.m`：配置；
- `kf_gins_ins_range_imuInforOpti.m`：主入口；
- `generate_baro_alt_sim.m`：气压高度仿真；
- `dataget.m`：数据准备；
- `input/`、`output/`：输入与结果。

### `旋转收缩方案/`

围绕轨迹旋转、尺度收缩和多组仿真对比的测距导航研究。

- `ins_range_fulll_version.m`、`ins_range_threetoge.m`：主要导航实验；
- `method.m`：方案验证；
- `cmp_4simu.m`、`cmp_simu_exper.m`：多仿真及仿真/实测对比；
- `func/`：自适应测距更新、轨迹数据计算与分列绘图；
- `input_simu*`：多组仿真输入和生成脚本；
- `output/`：结果。

### `可观测度分析/`

研究单/多/移动信标和不同轨迹构型的可观测性。

- `calculate_log_det_FIM*.m`：以 Fisher 信息矩阵对单、双、三信标或整段轨迹评分；
- `calculate_instantaneous_observability.m`：瞬时可观测度；
- `single_straight_line.m`、`two_straight_line.m`、`three_straight_line.m`：直线构型；
- `circle_constraint.m`、`moving_beacon.m`、`mutiple_beacons.m`：圆周、移动和多信标方案；
- `exper_data.m`、`all_ways.m`：实测/综合对比。

### `new_惯导试验/`

较新的综合研究区，包含仿真、实测、FGO、可观测度和全流程批处理。

- 根目录 `ins_range_1.m`、`ins_range_toge.m`、`ins_exper_range_m.m`：测距/INS 实验；
- `ProcessConfigforSemiPhy_all.m`：半实物/综合配置；
- `rangedataget_back.m`：反向或回程测距数据准备；
- `result_simu.m`、`result_exper.m`、`result_plot.m`、`output_fig.m`、`cmp_results.m`：结果分析；
- `simu/`：仿真轨迹、数据生成和高度/可观测度实验；
- `exper/`：纯惯导、LBL、不同信标组合和实测实验；
- `all_process/`：批量配置、全流程运行、失距场景和多轮误差汇总；
- `beacon_observability_func/`：信标观测记录、传播、分析和导出，附 `README_zh.md`；
- `C++_cmp/`：MATLAB 与 C++ 结果对比；
- `GRU/`：当前为空，可能是预留目录；
- `fgo_navigation/`：当前 FGO/KF 对比的集中实现。

`new_惯导试验/fgo_navigation/` 的关键入口：

| 文件 | 作用 |
| --- | --- |
| `kf_gnss_ins.m` | 1 Hz GNSS + 100 Hz INS 的 EKF 基线。 |
| `kf_range_ins.m` | 高频高度 + 低频测距的 EKF RANGE/INS 基线。 |
| `kf_range_ins_height1s.m` | 高度降采样为 1 s 更新的 EKF 版本。 |
| `fgo_gnss_ins_demo.m`、`fgo_gnss_ins_standard.m` | GNSS/INS 因子图实验。 |
| `fgo_range_ins.m` | KF 初值 + IMU/高度/距离因子 + Gauss-Newton 批量优化。 |
| `fgo_range_ins_fixedlag_full15_revised.m` | 15 维全状态固定滞后版本。 |
| `fgo_range_ins_realtime_int15.m` | 15 维预积分、固定滞后、近实时滑窗版本。 |
| `result_plot_1.m` | KF/FGO/RTS 等结果对比。 |

### `专利实验/`

面向专利材料的简化实验闭环：`patent_Configsimu.m` 配置，`patent_simu.m` 生成仿真，`patent_kf_gins.m` 执行滤波；`input/` 和 `output/` 保存输入与结果。

### `graduation/DR_INS/`

毕业设计/论文方向的航位推算（DR）、测距与 PSINS 对比。

- `main_DR_RANGE.m`、`main_DR_RANGE_4beacon.m`：单/多信标 DR_RANGE 入口；
- `PSINS_Dr_RangeDr.m`：PSINS 方案对比；
- `dataget.m`：数据准备；
- `result_plot.m`：结果绘制；
- `function/`：DR 初始化、机械编排、传播、反馈、测距更新、配置和输出；
- `doc/`：该子项目已有运行、数据格式和工作梳理文档；
- `input/`、`output*`：输入及不同实现的输出。

### `潜标位置标定/`

针对潜标/水声信标位置误差的两阶段标定、补偿与组合导航验证，是当前数据量最大的专题目录。

- `stage_1/`：短时标定。`calib.m`、`calib_1.m` 估计信标/角度参数，`INS_range_m.m` 执行 INS/测距处理，`dataget_1.m` 准备数据；
- `stage_2/`：将第一阶段估计用于长航段组合导航和二次迭代。`stage2_integration_nav.m` 为主入口，`estimate_angle*.m`、`result_usbl_true_cali.m` 和对比绘图脚本完成评估；
- `func/`：配置、纯惯导、测距重构、迭代位置比较、标定成果可视化和 Excel 导出；
- `Non-idealization/`：非理想误差场景；
- `old/`：旧版仿真、LBL 定位与轨迹/基站生成；
- `result_file/`：集中结果；
- `stage_1/data*`、`stage_2/data/`：不同直线/圆/方形轨迹、阵列中心/信标参考点场景的输入、中间量和输出。

典型两阶段链路：

```text
stage_1 短航段 IMU + 测距 + 真值
  → 标定潜标位置/安装角等误差
  → range_reconstruct* 重构或补偿测距
  → stage_2 长航段组合导航
  → 迭代更新参考参数
  → show_result* / Excel / PNG 评估
```

### 其他目录

| 目录 | 作用 |
| --- | --- |
| `application/` | 当前仅 `cmpuWeight.m`，用于权重计算/比较的独立应用函数。 |
| `测距/` | `range.m` 及若干测距数据文件，属于早期或独立测距试验。 |
| `else/` | 上游项目的 `README.md`、`README_CN.md`、GPL `LICENSE` 与参考文献 `ref.bib`。 |

## 7. 文件命名与角色判断

| 命名模式 | 一般含义 |
| --- | --- |
| `ProcessConfig*.m`、`Config*.m` | 数据路径、初值、噪声、时间范围和功能开关。 |
| `dataget*.m`、`rangedataget*.m` | 原始数据读取、格式转换、量测仿真或对齐。 |
| `kf_*`、`ins_*`、`main_*` | 可直接运行的滤波/导航主脚本。 |
| `fgo_*` | 因子图优化主脚本或实验版本。 |
| `my*Update.m` | 量测更新函数。 |
| `myInsPropagate*.m` | 误差状态和协方差预测。 |
| `myErrorFeedback*.m` | 闭环反馈。 |
| `plot_*`、`result_*`、`cmp_*`、`calc_*` | 绘图、统计、对比与评价。 |
| `Copy_of_*`、`*_backup.m`、`*.asv`、`old/` | 备份、MATLAB 自动保存或历史实现。 |
| `input/`、`input_*` | 原始或预处理输入。 |
| `output/`、`result_file/`、`fig/` | 计算结果、表格和图件。 |

## 8. 运行建议与已知注意事项

1. 在 MATLAB 中先运行根目录 `pathinit.m`，或手动执行 `addpath(genpath(repoRoot))`。`pathinit.m` 会修改并保存 MATLAB path，若不希望永久修改，可去掉 `savepath`。
2. 优先从某个专题的 `ProcessConfig*.m` 和主入口成对阅读；不同脚本对同名变量、状态维数和文件列格式的假设并不完全一致。
3. 多处脚本含 `D:\Github\KF-GINS-Matlab\...` 绝对路径，仓库移动到其他位置后必须修改。部分旧文件的中文注释在当前终端显示为乱码，可能是 GBK/UTF-8 编码不一致；不要直接批量转码后覆盖。
4. `input/` 和 `output/` 并非严格分离：部分目录保存了已生成结果，重新运行可能覆盖同名 `.nav`、`.txt`、`.xlsx` 或图片。
5. 运行主脚本前检查 `clear/clc/close all`、全局变量（如测距/深度噪声）、随机种子、时间范围、反馈开关和状态维数。
6. `Copy_of_*`、`*_backup.m`、`.asv`、`old/` 和 `New Folder/` 默认视为历史/临时实现；除非复现实验，不建议作为新开发基线。
7. `GINS-KF/ODONHCUpdate.m` 来源于上游框架，原始 README 提示 ODO/NHC 部分并非完整实现；使用前应核对观测模型和尺度因子状态。
8. 仓库中存在 PSINS 风格调用（如 `glvs`）；这些专题可能依赖未随本仓库统一说明的外部工具箱或已配置 MATLAB 路径。
9. 建议新实验采用“配置、输入、输出、绘图”四层结构，并避免继续在主脚本中增加绝对路径。

## 9. 建议的后续整理

- 将根目录 `else/README_CN.md` 恢复到仓库根目录并统一 UTF-8 编码；
- 为每个活跃专题补充一个简短 README，写清入口、输入列格式和期望输出；
- 将公共配置中的绝对路径改为基于 `mfilename('fullpath')` 的相对路径；
- 将历史副本、`.asv`、图片和大体积结果移入 `archive/` 或发布产物目录；
- 为 `function_zxy/Update/` 建立“状态维数—观测类型—反馈函数”对应表；
- 为主要入口增加小数据 smoke test，避免全量数据运行后才发现路径或维数错误。

---

本文档侧重代码导航，不替代各实验的参数说明或数据格式规范。对某一专题进行修改前，建议先阅读对应配置函数、主脚本、量测更新函数和结果评估脚本四部分。
